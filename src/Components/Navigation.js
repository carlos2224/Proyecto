import React from 'react';
import { render } from 'react-dom';

function Navigation() {
    render(); {
        return(
            <nav className="navbar navbnar-dark bg-dark">
                <a href="" className="text-white">
                    Tasks 
                </a>
            </nav>
        )
    }
}
export default Navigation;